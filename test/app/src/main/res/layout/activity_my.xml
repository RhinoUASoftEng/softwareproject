<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <Button
        android:onClick="loginPage"
        android:layout_width="1000dp"
        android:layout_height="wrap_content"
        android:id="@+id/login"
        android:text="Login"/>

    <Button
        android:onClick="createUserPage"
        android:layout_width="1000dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/login"
        android:id="@+id/createUser"
        android:text="Create An Account"/>
    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:id="@+id/test"
        android:layout_below="@+id/createUser"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignRight="@+id/createUser"
        android:layout_alignEnd="@+id/createUser"
        android:textColorHint="#ffff299f"
        android:hint="Put any value you want here"/>
    <Button
        android:onClick="sendToServer"
        android:layout_width="1000dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/test"
        android:text="Send Data to Server"/>
</RelativeLayout>

